{
  "$schema": "http://json-schema.org/draft-04/schema",
  "title": "DATS descriptive statistics schema",
  "description": "an annotation to provide key information about central tendency and variability about a dats.dimension",
  "type": "object",
  "properties": {
  "@context": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "object"
        }
      ]
    },
  "@type": { "type": "string", "enum": [ "Statistics" ]},
    "category": {
      "description": "A characteristic or property about the entity this object is associated with.",
      "type": "string",
      "enum": ["overall_count","missing_count","invalid_count","mix", "max","mode","mean","standard deviation"]
    },
    "value": {
           "type": "number"
    }
  },
  "additionalProperties": false,
  "required": ["category","value"]
}
  